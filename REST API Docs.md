# REST API Documentation
## 👤 Users (회원)

### 회원가입

`POST /users` 

서비스에 새로운 사용자를 등록한다. 이메일, 비밀번호 등 필수 정보를 입력받아 회원 계정을 생성하며 이미 존재하는 이메일 또는 닉네임이 있을 경우 회원가입에 실패한다.

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| email | string | ✅ | 회원의 이메일을 받는다. 또한 이메일 중복으로 가입을 허용하지 않는다. |
| name | string | ✅ | 회원의 이름을 받는다. |
| password | string | ✅ | 회원의 비밀번호를 받는다. 대문자와 특수기호는 반드시 들어가야 하며 글자는 최소8자이다. |
| nickname | string | ❌ | 회원의 닉네임을 받는다. 회원간의 닉네임 중복을 허락하지 않는다. |
| profile_image | string | ❌ | 회원의 프로필 사진을 받는다. |

**Request**

```json
{
  "email": "[user123@naver.com](mailto:user123@naver.com)",
  "name": "jmw",
  "password": "Jmw1234@@!",
  "nickname": "j0314",
  "profile_image": "[https://example.com/image.png](https://example.com/image.png)"
}
```

**Response (201 Created)**

```json
{
  "status": "success",
  "data": {
    "userId": "1",
    "email": "[user123@naver.com](mailto:user123@naver.com)",
    "name": "jmw",
    "nickname": "j0314",
    "created_at": "2026-01-04T12:00:00Z"
  }
}
```

**Response (409 충돌)**

```json
{
  "status": "error",
  "data": {
    "message": "닉네임이 중복되었습니다."
  }
}
```

---

### 로그인

`POST /auth/tokens`

등록된 사용자 계정으로 로그인을 수행한다. 사용자가 입력한 이메일과 비밀번호를 기반으로 로그인을 진행하며 인증에 성공할 경우 이후 인증이 필요한 API 요청에 사용할 수 있는 **Access Token**을 반환한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Content-Type | string | ✅ | application/json |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| email | string | ✅ | ID역할로써 가입시 입력한 이메일입니다. 이메일 형식을 만족해야 하며 대소문자는 구분하지 않는다. |
| password | string | ✅ | 가입시 설정한 비밀번호입니다. |

**Request**

```json
{
  "email": "[user123@naver.com](mailto:user123@naver.com)",
  "password": "Jmw1234@@!"
}
```

**Response (200 성공)**

```json
{
  "status": "success",
  "data": {
    "access_token": "dddddviHF8y9e8f",
    "token_type": "Bearer",
    "expires_in": 3600
  }
}
```

**Response (401 실패)**

```json
{
  "status": "error",
  "data": {
    "message": "이메일 또는 비밀번호가 일치하지 않습니다."
  }
}
```

---

### 내 프로필 조회

`GET /users/me`

Access Token을 통해 인증된 로그인 사용자의 본인 프로필 정보를 조회한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인 시 발급받은 Access Token을 Bearer {accessToken} 형식으로 전달합니다. |

**Response (200 성공)**

```json
{
  "status": "success",
  "data": {
    "email": "user123@naver.com",
    "name": "jmw",
    "nickname": "j0314",
    "profile_image": "https://example.com/image.png"
  }
}
```

---

### 프로필 수정

`PATCH /users/me`

로그인 한 사용자의 정보를 수정한다. 수정이 필요한 항목만 선택적으로 수정할 수 있고 닉네임이 중복일 경우에 수정이 불가능하다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인 시 발급받은 Access Token을 Bearer {accessToken} 형식으로 전달한다. |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| nickname | string | ❌ | 변경할 사용자의 닉네임이며 중복을 허락하지 않는다. |
| password | string | ❌ | 사용자의 비밀번호를 변경한다. 대문자와 특수기호는 반드시 들어가야 하며 글자는 8자~20자리 사이여야 한다. |
| profile_image | string | ❌ | 사용자의 프로필 사진을 변경한다. |

**Request**

```json
{
  "nickname": "jjj",
  "password": "Jmw5678~~",
  "profile_image": "https://j.com/image.png"
}
```

**Response (200 성공)**

```json
{
  "status": "success",
  "data": {
    "nickname": "jjj",
    "profile_image": "https://j.com/image.png"
  }
}
```

**Response (409 충돌)**

```json
{
  "status": "error",
  "data": {
    "message": "닉네임이 중복되었습니다."
  }
}
```

---

### 회원 탈퇴

`DELETE /users/me`

로그인한 사용자의 계정을 삭제한다. 탈퇴가 완료되면 해당 사용자는 더 이상 서비스를 이용할 수 없다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인 시 발급받은 Access Token을 Bearer {accessToken} 형식으로 전달한다. |

**Response (204 No Content)**

```
(응답 본문 없음)
```

---

### 특정 회원 조회

`GET /users/{userId}`

특정 회원의 공개 프로필 정보를 조회한다. 중요한 정보는 포함되지 않는다.

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| userId | number | ✅ | 조회할 사용자의 고유 식별자이다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": {
    "userId": "1",
    "nickname": "kds",
    "profile_image": "https://example.com/image.png"
  }
}
```

**Response (404 Not Found)**

```json
{
  "status": "error",
  "data": {
    "message": "해당 사용자를 찾을 수 없습니다."
  }
}
```

---

## 📝 Posts (게시글)

### 게시글 목록 조회

`GET /posts`

서비스에 등록된 게시글 목록을 페이지 단위로 조회한다. 한 번의 요청으로 모든 게시글을 조회하지 않고 페이지네이션을 적용한다. 공개 게시글이기 때문에 로그인하지 않아도 게시글을 확인할 수 있다.

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| page | number | ❌ | 조회할 페이지 번호이다. (기본값: 1) |
| limit | number | ❌ | 한 페이지당 게시글의 수이다. 최소 1개부터 최대 100개까지 가능하다. (기본값: 20) |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "postId": "1",
      "title": "처음 올리는 게시글",
      "nickname": "jjj"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

---

### 게시글 검색

`GET /posts/search`

게시글 제목 또는 내용에 포함된 키워드를 기준으로 게시글을 검색한다. 검색은 제목, 내용, 작성자 닉네임으로만 검색할 수 있게 한다. 검색 결과는 **검색된 게시글 전체**를 반환한다.

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| keyword | string | ✅ | 검색할 키워드이다. 제목이나 내용, 닉네임으로만 검색할 수 있게 한다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": {
    "postId": "1",
    "title": "처음 올리는 게시글",
    "nickname": "jjj"
  }
}
```

---

### 게시글 정렬

`GET /posts`

게시글 목록을 정렬하여 반환한다. 최신순을 기본값으로 하며 좋아요순이나 수정일 등을 기준으로 지정할 수 있다.

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| sort | string | ❌ | 결과 정렬 기준 필드이다. 기본적으로 내림차순(최신순)으로 정렬되며 기준을 수정일 또는 좋아요 기준으로 지정할 수 있다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "postId": "1",
      "title": "처음 쓰는 게시글",
      "nickname": "jjj",
      "createdAt": "2026-01-02T10:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

---

### 게시글 상세조회

`GET /posts/{postId}`

특정 게시글의 상세 정보를 조회한다. **로그인한 사용자만 접근 가능하며** 조회 시 조회수가 자동으로 1 증가한다. 응답되는 정보에는 게시글 제목, 내용, 작성자, 생성일, 수정일, 조회수, 좋아요 수 등이 포함된다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 사용자가 로그인을 했는지 안했는지를 확인하는 인증용이다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | string | ✅ | 조회할 게시글의 고유 id이다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": {
    "postId": "1",
    "title": "처음 쓰는 게시글",
    "content": "3주차 과제중",
    "nickname": "jjj",
    "createdAt": "2026-01-04T12:00:00Z",
    "updatedAt": "2026-01-05T08:00:00Z",
    "views": 121,
    "Likes": 2
  }
}
```

---

### 게시글 작성

`POST /posts`

로그인을 한 사용자가 새 게시글을 작성한다. 로그인하지 않으면 작성할 수 없고 토큰에서 확인한 회원 정보를 기반으로 게시글 작성자를 식별한다. 또한 새 게시글이기 때문에 조회수, 좋아요는 0으로 초기화한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 사용자가 로그인을 하였는지 확인한다. |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| title | string | ✅ | 게시글 제목이며 빈 값일 수는 없다. |
| content | string | ✅ | 게시글 본문 내용이며 빈 값일 수 없다. |

**Request**

```json
{
  "title": "처음 쓰는 게시글",
  "content": "3주차 과제중.."
}
```

**Response (201 Created)**

```json
{
  "status": "success",
  "data": {
    "title": "처음 쓰는 게시글",
    "content": "3주차 과제중..",
    "nickname": "jjj",
    "created_at": "2026-01-04T12:00:00Z",
    "views": 0,
    "likes": 0
  }
}
```

**Response (400 잘못된 요청)**

```json
{
  "status": "error",
  "data": {
    "message": "제목 또는 본문을 입력해야 합니다."
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

---

### 게시글 수정

`PATCH /posts/{postId}`

특정 게시글의 제목 또는 본문을 수정한다. 게시글을 작성한 작성자만 수정할 수 있으며 수정하지 않은 필드는 기존 값을 유지한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인한 사용자인지 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | string | ✅ | 수정할 게시글의 고유 id이다. |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| title | string | ❌ | 수정할 게시글 제목이며 수정하지 않으면 기존 제목을 유지한다. |
| content | string | ❌ | 수정한 게시글 본문이며 수정하지 않으면 기존 본문을 유지한다. |

**Request**

```json
{
  "title": "수정된 제목",
  "content": "수정된 본문"
}
```

**Response (200 성공)**

```json
{
  "status": "success",
  "data": {
    "postId": "1",
    "title": "수정된 제목",
    "content": "수정된 내용",
    "nickname": "jjj",
    "createdAt": "2026-01-04T12:00:00Z",
    "updatedAt": "2026-01-07T15:30:00Z",
    "view": 100,
    "likes": 2
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (404 Not Found)**

```json
{
  "status": "error",
  "data": {
    "message": "존재하지 않는 게시글입니다."
  }
}
```

---

### 게시글 삭제

`DELETE /posts/{postId}`

특정 게시글을 삭제한다. 해당 게시글을 작성한 작성자만 삭제할 수 있으며 삭제가 완료된 게시글을 더 이상 조회할 수 없다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인한 사용자인지 확인하며 서버는 토큰을 검증하여 삭제 권한을 확인한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | string | ✅ | 삭제할 게시글의 고유 id이다. |

**Response (204 No Content)**

```
(응답 본문 없음)
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (403 권한 없음)**

```json
{
  "status": "error",
  "data": {
    "message": "게시글을 삭제할 권한이 없습니다."
  }
}
```

---

### 내가 쓴 게시글 목록

`GET /posts/me`

로그인을 한 사용자가 작성한 게시글 목록을 조회한다. 게시글 목록은 페이지네이션이 적용된다. 로그인을 하지 않으면 접근할 수 없다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| page | number | ❌ | 조회할 페이지 번호이다. |
| limit | number | ❌ | 한 페이지당 조회할 게시글 수이며 최소 1개부터 최대 10개까지 가능하다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "postId": "1",
      "title": "첫 번째 게시글",
      "content": "게시글 내용입니다.",
      "nickname": "jjj",
      "created_at": "2026-01-04T12:00:00Z",
      "views": 15,
      "likes": 2
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 100
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

---

## 💬 Comments (댓글)

### 댓글 목록 조회

`GET /posts/{postId}/comments`

특정 게시글에 작성된 댓글 목록을 조회한다. 댓글 조회는 로그인 하지 않은 사람도 조회할 수 있으며 댓글 목록은 페이지네이션을 적용한다.

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | number | ✅ | 댓글 목록을 조회할 게시글의 고유 식별자이다. |

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| page | number | ❌ | 조회할 게시글의 댓글 목록 페이지 번호이다. 최솟값은 1이다. |
| limit | number | ❌ | 한 페이지에 조회할 댓글 수이다. 최소 1개부터 최대 20개까지 가능하다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "commentId": "1",
      "content": "댓글내용",
      "nickname": "jjj",
      "created_at": "2026-01-04T12:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

---

### 댓글 작성

`POST /posts/{postId}/comments`

특정 게시글에 댓글을 작성한다. 로그인한 사용자만 댓글을 작성할 수 있으며 댓글의 공백으로 허용하지 않는다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | number | ✅ | 댓글을 작성할 게시글의 고유 식별자이다. |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| content | string | ✅ | 댓글 내용이다. 공백은 허용하지 않는다. |

**Request**

```json
{
  "content": "댓글 내용"
}
```

**Response (201 Created)**

```json
{
  "status": "success",
  "data": {
    "commentId": 1,
    "postId": 2,
    "nickname": "jjj",
    "content": "댓글 내용",
    "created_at": "2026-01-04T12:00:00Z"
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (400 잘못된 요청)**

```json
{
  "status": "error",
  "data": {
    "message": "댓글을 작성하지 않았습니다."
  }
}
```

---

### 댓글 수정

`PATCH /comments/{commentId}`

특정 댓글의 내용을 수정한다. **로그인한 사용자만 접근 가능하며 본인이 작성한 댓글만 수정할 수 있다.**

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| commentId | number | ✅ | 수정할 댓글의 고유 식별자이다. |

**Request Body**

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| content | string | ✅ | 수정할 댓글 내용이다. 공백은 허용하지 않는다. |

**Request**

```json
{
  "content": "댓글 내용 수정"
}
```

**Response (200 성공)**

```json
{
  "status": "success",
  "data": {
    "commentId": 1,
    "postId": 2,
    "nickname": "jjj",
    "content": "댓글 내용 수정",
    "updatedAt": "2026-01-09T10:30:00Z"
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (403 권한 없음)**

```json
{
  "status": "error",
  "data": {
    "message": "본인이 작성한 댓글만 수정할 수 있습니다."
  }
}
```

---

### 댓글 삭제

`DELETE /comments/{commentId}`

특정 댓글을 삭제한다. **로그인한 사용자만 접근 가능하며 본인이 작성한 댓글만 삭제할 수 있다.** 삭제한 댓글은 더 이상 조회되지 않는다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| commentId | number | ✅ | 삭제할 댓글의 고유 식별자이다. |

**Response (204 No Content)**

```
(응답 본문 없음)
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (403 권한 없음)**

```json
{
  "status": "error",
  "data": {
    "message": "본인이 작성한 댓글만 삭제할 수 있습니다."
  }
}
```

---

### 내가 쓴 댓글 목록

`GET /comments/me`

로그인 한 사용자가 직접 작성한 댓글 목록만 조회한다. 댓글은 최신순으로 정렬되며 페이지네이션을 적용한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| page | number | ❌ | 조회할 댓글 목록 페이지 번호이다. 최솟값은 1이다. |
| limit | number | ❌ | 한 페이지에 조회할 댓글 수이다. 최소 1개부터 최대 20개까지 가능하다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "commentId": "1",
      "content": "댓글내용 수정",
      "nickname": "jjj",
      "created_at": "2026-01-04T12:00:00Z",
      "updatedAt": "2026-01-07T09:30:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

---

## 👍 Likes (좋아요)

### 좋아요 등록

`POST /posts/{postId}/likes`

로그인을 한 사용자가 특정 게시글에 좋아요를 누른다. 이미 해당 게시글에 좋아요를 누른 경우 중복을 허락하지 않는다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인한 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | number | ✅ | 좋아요를 누를 게시글의 고유 식별자이다. |

**Response (201 Created)**

```json
{
  "status": "success",
  "data": {
    "message": "좋아요가 등록되었습니다."
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (409 충돌 발생)**

```json
{
  "status": "error",
  "data": {
    "message": "이미 좋아요를 누른 게시글입니다."
  }
}
```

---

### 좋아요 취소

`DELETE /posts/{postId}/likes`

로그인을 한 사용자가 특정 게시글에 등록한 좋아요를 취소한다. 해당 게시글에 좋아요를 누르지 않았을 경우 취소할 수 없다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인한 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | number | ✅ | 좋아요를 취소할 게시글의 고유 식별자이다. |

**Response (204 No Content)**

```
(응답 본문 없음)
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

**Response (409 충돌 발생)**

```json
{
  "status": "error",
  "data": {
    "message": "좋아요가 등록되지 않았습니다."
  }
}
```

---

### 좋아요 상태 확인

`GET /posts/{postId}/likes`

특정 게시글에 대해 **현재 로그인한 사용자가 좋아요를 눌렀는지 여부를 확인하고 해당 게시글의 총 좋아요 수를 확인한다.**

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인한 사용자를 식별한다. |

**Path Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| postId | number | ✅ | 좋아요 상태를 확인할 게시글의 고유 식별자 |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": {
    "liked": true,
    "likeCount": 23
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```

---

### 내가 좋아요한 게시글 목록

`GET /posts/liked`

로그인한 사용자가 **좋아요를 누른 게시글 목록**을 조회한다. 게시글은 좋아요를 누른 시점을 기준으로 **최신순으로 정렬**되며 페이지네이션을 적용한다.

**Request Headers**

| 헤더 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| Authorization | string | ✅ | 로그인된 사용자를 식별한다. |

**Query Parameters**

| 파라미터 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| page | number | ❌ | 조회할 페이지 번호이다. 최솟값은 1이다. |
| limit | number | ❌ | 한 페이지에 조회할 게시글 수이다. 최소 1개부터 최대 20개까지 가능하다. |

**Response (200 OK)**

```json
{
  "status": "success",
  "data": [
    {
      "postId": "1",
      "title": "게시글 제목",
      "nickname": "jjj",
      "liked_at": "2026-01-04T12:00:00Z",
      "likeCount": 22
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

**Response (401 인증 필요)**

```json
{
  "status": "error",
  "data": {
    "message": "로그인이 필요합니다."
  }
}
```